

<div id="allgenres">

<p id="notice"><%= notice %></p>
<h1>Genres &hearts;</h1>
<div id="genres">


<table class="table table-striped table-hover " id="table1">
  <thead>
    <tr>
      <th>Name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @genres.each do |genre| %>
      <tr>
        <td><%= genre.name %></td>
        <td><%= link_to 'Show', genre %></td>
        <td><%= link_to 'Edit', edit_genre_path(genre) %></td>
        <td><%= link_to 'Destroy', genre, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<br>

<center><button type="button" class="btn btn-warning"><%= link_to 'New Genre', new_genre_path %></button></center>
</div>

</div>

<center>
<%= will_paginate @blogs %>
</center>